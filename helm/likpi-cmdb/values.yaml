replicaCount: 3

images:
  backend: "likpi/cmdb-backend:latest"
  frontend: "likpi/cmdb-frontend:latest"

database:
  user: "cmdb_user"
  # password: "" # SECURITY RULE: Do not set passwords in values.yaml. 
  # You MUST pass this during installation via: --set database.password=YOUR_SECURE_PASSWORD
  storage: "10Gi"
